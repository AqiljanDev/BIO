<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Неактивное состояние: используем стандартный фон с рамкой -->
    <item android:drawable="@android:drawable/editbox_background_normal" android:state_focused="false"/>

    <!-- Активное состояние: прозрачный фон (без подсветки) -->
    <item android:drawable="@android:color/transparent" android:state_focused="true"/>
</selector>
